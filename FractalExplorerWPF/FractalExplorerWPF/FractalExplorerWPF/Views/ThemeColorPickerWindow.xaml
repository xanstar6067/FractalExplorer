<Window x:Class="FractalExplorerWPF.Views.ThemeColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:theme="clr-namespace:FractalExplorerWPF.Theming"
        Title="Выбор цвета" Width="460" Height="410" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="Preview" Height="72" CornerRadius="7" BorderThickness="1"
                BorderBrush="{DynamicResource Theme.BorderBrush}" theme:ThemeContract.IgnoreAudit="True"/>

        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="35"/><ColumnDefinition Width="*"/><ColumnDefinition Width="45"/></Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="A"/><Slider x:Name="AlphaSlider" Grid.Column="1" Maximum="255" ValueChanged="ChannelSlider_OnValueChanged"/>
            <TextBlock x:Name="AlphaValue" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="35"/><ColumnDefinition Width="*"/><ColumnDefinition Width="45"/></Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="R"/><Slider x:Name="RedSlider" Grid.Column="1" Maximum="255" ValueChanged="ChannelSlider_OnValueChanged"/>
            <TextBlock x:Name="RedValue" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="35"/><ColumnDefinition Width="*"/><ColumnDefinition Width="45"/></Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="G"/><Slider x:Name="GreenSlider" Grid.Column="1" Maximum="255" ValueChanged="ChannelSlider_OnValueChanged"/>
            <TextBlock x:Name="GreenValue" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="4" Margin="0,8,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="35"/><ColumnDefinition Width="*"/><ColumnDefinition Width="45"/></Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="B"/><Slider x:Name="BlueSlider" Grid.Column="1" Maximum="255" ValueChanged="ChannelSlider_OnValueChanged"/>
            <TextBlock x:Name="BlueValue" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>

        <StackPanel Grid.Row="5" Margin="0,16,0,0">
            <TextBlock Margin="0,0,0,4" Foreground="{DynamicResource Theme.SecondaryTextBrush}" Text="Значение #AARRGGBB"/>
            <TextBox x:Name="HexBox" FontFamily="Consolas" TextChanged="HexBox_OnTextChanged"/>
            <TextBlock x:Name="ValidationText" Margin="0,5,0,0" Foreground="{DynamicResource Theme.SecondaryTextBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Margin="0,18,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="105" Margin="4" Content="Отмена" IsCancel="True"/>
            <Button Width="105" Margin="4" Content="Выбрать" IsDefault="True" Click="Accept_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
