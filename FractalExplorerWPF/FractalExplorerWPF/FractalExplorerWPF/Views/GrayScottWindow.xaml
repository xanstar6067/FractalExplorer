<Window x:Class="FractalExplorerWPF.Views.GrayScottWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:theme="clr-namespace:FractalExplorerWPF.Theming"
        Title="Gray–Scott reaction–diffusion" Width="1180" Height="780" MinWidth="920" MinHeight="620"
        WindowStartupLocation="CenterOwner" KeyDown="Window_OnKeyDown" Closing="Window_OnClosing">
    <Grid Background="{DynamicResource Theme.BaseBackgroundBrush}">
        <Grid.ColumnDefinitions><ColumnDefinition x:Name="ControlsColumn" Width="330"/><ColumnDefinition/></Grid.ColumnDefinitions>
        <Border x:Name="ControlsHost" Margin="10" Padding="14" Background="{DynamicResource Theme.PanelBackgroundBrush}"
                BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" CornerRadius="7">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="Gray–Scott reaction–diffusion"/>
                    <TextBlock Margin="0,3,0,10" Foreground="{DynamicResource Theme.SecondaryTextBrush}" Text="Двухкомпонентная пространственная система"/>

                    <TextBlock Margin="0,0,0,3" Text="Готовый режим"/>
                    <ComboBox x:Name="PresetBox" SelectionChanged="Preset_OnChanged"/>

                    <GroupBox Margin="0,10,0,0" Header="Уравнение">
                        <StackPanel Margin="7">
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Диффузия U"/><TextBox x:Name="DiffusionUBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Диффузия V"/><TextBox x:Name="DiffusionVBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Подача F"/><TextBox x:Name="FeedBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Удаление K"/><TextBox x:Name="KillBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Шаг времени Δt"/><TextBox x:Name="DeltaTimeBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Margin="0,8,0,0" Header="Сетка и затравка">
                        <StackPanel Margin="7">
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Размер сетки"/><TextBox x:Name="GridSizeBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <TextBlock Margin="0,5,0,3" Text="Начальное состояние"/>
                            <ComboBox x:Name="SeedModeBox" SelectionChanged="Parameter_OnChanged">
                                <ComboBoxItem Content="Квадрат в центре"/><ComboBoxItem Content="Случайные пятна"/>
                                <ComboBoxItem Content="Кольцо"/><ComboBoxItem Content="Шум по полю"/>
                            </ComboBox>
                            <Grid Margin="0,5,0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Число пятен"/><TextBox x:Name="SeedCountBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Радиус затравки"/><TextBox x:Name="SeedRadiusBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Seed генератора"/><TextBox x:Name="RandomSeedBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Радиус кисти"/><TextBox x:Name="BrushRadiusBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Margin="0,8,0,0" Header="Анимация и цвет">
                        <StackPanel Margin="7">
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Шагов на кадр"/><TextBox x:Name="StepsPerFrameBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Целевая частота"/><ComboBox x:Name="TargetFpsBox" Grid.Column="1" SelectionChanged="TargetFps_OnChanged"><ComboBoxItem Tag="30" Content="30 FPS"/><ComboBoxItem Tag="60" Content="60 FPS"/></ComboBox></Grid>
                            <TextBlock Margin="0,5,0,3" Text="Отображаемое поле"/>
                            <ComboBox x:Name="FieldModeBox" SelectionChanged="Parameter_OnChanged"><ComboBoxItem Content="Концентрация V"/><ComboBoxItem Content="Концентрация U"/><ComboBoxItem Content="Разность U − V"/></ComboBox>
                            <Grid Margin="0,5,0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Минимум диапазона"/><TextBox x:Name="RangeMinimumBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="116"/></Grid.ColumnDefinitions><TextBlock VerticalAlignment="Center" Text="Максимум диапазона"/><TextBox x:Name="RangeMaximumBox" Grid.Column="1" TextChanged="Parameter_OnChanged"/></Grid>
                            <CheckBox x:Name="ReversePaletteBox" Margin="0,5,0,2" Content="Инвертировать палитру" Click="PaletteMapping_OnChanged"/>
                            <Grid Margin="0,5,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="78"/></Grid.ColumnDefinitions>
                                <Button Content="Палитры Gray–Scott…" Click="Palette_OnClick"/>
                                <Border x:Name="PalettePreview" Grid.Column="1" Margin="5" Height="25" CornerRadius="4" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" theme:ThemeContract.IgnoreAudit="True"/>
                            </Grid>
                        </StackPanel>
                    </GroupBox>

                    <TextBlock x:Name="PendingText" Margin="0,8,0,0" Foreground="{DynamicResource Theme.SecondaryTextBrush}" Text="Параметры изменены — примените их" Visibility="Collapsed"/>
                    <Button Margin="0,6,0,0" Content="Применить и перезапустить" Click="Reset_OnClick"/>
                    <UniformGrid Columns="2" Margin="0,3,0,0">
                        <Button x:Name="RunButton" Margin="0,0,3,0" Content="Пауза" Click="RunPause_OnClick"/>
                        <Button x:Name="StepButton" Margin="3,0,0,0" Content="Один кадр" Click="Step_OnClick"/>
                    </UniformGrid>
                    <Button Content="Новая случайная затравка" Click="Randomize_OnClick"/>
                    <Button Content="Менеджер сохранений" Click="Saves_OnClick"/>
                    <Button Content="Сохранить текущий кадр" Click="Export_OnClick"/>
                    <TextBlock x:Name="StatusText" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="{DynamicResource Theme.SecondaryTextBrush}" Text="Подготовка симуляции…"/>
                    <TextBlock Margin="0,9,0,0" TextWrapping="Wrap" Foreground="{DynamicResource Theme.DisabledTextBrush}"
                               Text="ЛКМ по полю добавляет реагент V. Сетка замкнута по краям. Частота 30/60 FPS является целевой и автоматически ограничивается скоростью расчёта."/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Border x:Name="CanvasHost" Grid.Column="1" Margin="0,10,10,10" Background="Black" ClipToBounds="True"
                BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" CornerRadius="7" theme:ThemeContract.IgnoreAudit="True"
                MouseLeftButtonDown="CanvasHost_OnMouseLeftButtonDown" MouseMove="CanvasHost_OnMouseMove" MouseLeftButtonUp="CanvasHost_OnMouseLeftButtonUp">
            <Grid>
                <Image x:Name="FrameImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="12" Padding="10,6" CornerRadius="5" Background="#B0000000">
                    <TextBlock x:Name="FrameBadgeText" Foreground="White" Text="0 FPS · шаг 0"/>
                </Border>
                <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="12" Padding="10,6" CornerRadius="5" Background="#90000000">
                    <TextBlock Foreground="White" Text="ЛКМ — добавить реагент V"/>
                </Border>
            </Grid>
        </Border>
        <Button x:Name="ToggleButton" Grid.Column="1" Style="{StaticResource FractalPanelToggleButtonStyle}" Content="✕"
                ToolTip="Скрыть панель параметров" Click="Toggle_OnClick"/>
    </Grid>
</Window>
