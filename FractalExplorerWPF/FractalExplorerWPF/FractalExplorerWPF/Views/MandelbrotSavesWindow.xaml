<Window x:Class="FractalExplorerWPF.Views.MandelbrotSavesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:theme="clr-namespace:FractalExplorerWPF.Theming"
        Title="Менеджер сохранений" Width="820" Height="520" MinWidth="700" MinHeight="430"
        WindowStartupLocation="CenterOwner" Icon="../Assets/Icons/Save.ico">
    <Grid Margin="12">
        <Grid.ColumnDefinitions><ColumnDefinition Width="270"/><ColumnDefinition Width="18"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBox x:Name="SaveNameBox" Padding="7" Margin="0,0,0,8"/>
            <ListBox x:Name="SavesList" Grid.Row="1" DisplayMemberPath="SaveName" SelectionChanged="SavesList_OnSelectionChanged"/>
            <UniformGrid Grid.Row="2" Columns="2" Margin="0,8,0,0">
                <Button Margin="2" Content="Сохранить" Click="Save_OnClick"/>
                <Button Margin="2" Content="Удалить" Click="Delete_OnClick"/>
            </UniformGrid>
        </Grid>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Border Background="Black" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" CornerRadius="5"
                    theme:ThemeContract.IgnoreAudit="True">
                <Image x:Name="PreviewImage" Stretch="Uniform"/>
            </Border>
            <TextBlock x:Name="DetailsText" Grid.Row="1" Margin="0,10" TextWrapping="Wrap"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="110" Margin="4" Content="Загрузить" Click="Load_OnClick"/>
                <Button Width="110" Margin="4" Content="Закрыть" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
